#include<iostream>
#include<string>
#include<vector>


void Ans(vector<string> v)
{
  ;
}

int main()
{
  int n;
  string s;
  vector<string> v;
  for(;;)
  {
    cin << n;
    cin.get();
    if(b == 0)
    {
      break;
    }
    v.clear();
    for(int i = 0; i < n; i++)
    {
      getline(cin,s);
      v.push_back(s);
    }
    Ans(v);
  }
  return 0;
}
